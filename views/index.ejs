<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>Product Index</title>
    </head>
    <body>
        <h1>Products index page</h1>
        <nav>
            <a href="/products/new">Add a New Product</a>
        </nav>
        <ol>

        <% for(let i = 0; i < allProducts.length; i++){ %>
            <div>
                <h2 class="product_name"><%= allProducts.name %> </h2>
                <a href="/products/<%=i%>">
                <img src="<%= allProducts[i].img%>"></a>
                <p>Price: $<%= allProducts[i].price%></p>
                <p>Quantity: <%= allProducts[i].qty%></p>
                <form action="/products/<%=allProducts._id%>?_method=DELETE" method="POST">
                <input class= "delete" type="submit" value="Delete">
                </form>
            </div>
        <% } %> 
    </body>